#!/bin/bash

# this script updates gflist.txt with all google fonts available
# to use this you must set GOOGLE_API_KEY env variable

echo "`curl -s "https://www.googleapis.com/webfonts/v1/webfonts?key=$GOOGLE_API_KEY&sort=alpha" | jq -r '.items[].family'`\n" > gflist.txt
echo "Update done"